#pragma once

class Midpoint {
public:
	void putPixel(int cx, int cy, int x, int y);
	void drawCircle(int cx, int cy, int r);
    void circleMidpointVideo(int cx, int cy, int r);
    void circleMidpointBook(int cx, int cy, int r);
    void drawLine(int x0, int y0, int x1, int y1);
    #pragma once

    class Draw {
    public:
        Draw() {}
        // Add drawing-related methods here if needed in the future
    };
};

